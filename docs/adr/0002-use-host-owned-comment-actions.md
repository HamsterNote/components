# 评论数据采用宿主持有的分动作契约

Pro 评论组件接收宿主提供的评论数据，并通过可异步的新增、回复、编辑和删除回调分别提交用户动作；组件只管理输入、展开和提交状态，不生成业务 ID、不直接请求固定后端，也不把整棵评论树作为一次变更回传。新增能力由宿主按评论上下文提供，回复、编辑和删除能力由宿主逐条评论提供，组件不根据作者身份推断权限。这样宿主可以按动作接入持久化、权限校验与失败处理，而评论组件仍可跨业务复用。
